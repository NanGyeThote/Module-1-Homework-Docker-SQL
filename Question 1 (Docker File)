# This is Docker File !
FROM python:3.12.8

RUN pip install pandas

ENTRYPOINT [ "bash" ]

# ------- Docker Command To test the PIP Version ----------------

# I run docker build -t test1:pandas .
# Then I run docker run -it test:pandas
# I tried pip --version then I got the value of 24.3.1 in python 3.12.8
